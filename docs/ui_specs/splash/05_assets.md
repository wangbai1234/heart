# 05 资产清单 — Splash Screen（启动屏）

本文件覆盖 Splash 页资源真相。旧版“拆成背景 + 心形 + 云朵 + 字标”的重建建议失效。

## 1. 必须引用的复杂资源

| 用途 | 绝对路径 | 使用方式 |
|---|---|---|
| Splash 全屏图 | `/Users/wanglixun/heart/assets/backgrounds/静态加载页面.png` | 整屏展示，`cover`，锚点 `center center` |

## 2. 使用规则

- Splash 必须直接使用 `静态加载页面.png`
- 图中的背景、心形、字标、tagline、三点全部视为同一张完整设计资源
- 不允许为了“组件化”而拆开后重画
- 不允许重新生成任意 Splash 子元素

## 3. 允许的工程处理

- 允许整张图淡入淡出
- 允许做全屏过渡
- 允许在不同设备上做等比 `cover`

## 4. 禁止事项

- 禁止用 CSS 自己重做 Splash
- 禁止把三点做成单独代码组件代替设计图内现成表现
- 禁止替换字标、tagline、心形图
