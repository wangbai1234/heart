# 02 Layout — 反馈组件三联布局规范

## 画布总体布局

```
画布: 1536 × 1024 px
背景色: #FFF0F3（浅樱花粉）

┌─────────────────────────────────────────────────────────────────────┐
│  canvas 1536×1024                                                     │
│                                                                       │
│  padding-top: ≈40px     padding-bottom: ≈80px（含底部标注区）           │
│  padding-left: ≈48px    padding-right: ≈48px                         │
│                                                                       │
│  ┌────────────┐   gap≈64px   ┌────────────┐   gap≈64px   ┌────────────┐ │
│  │ Phone 1    │              │ Phone 2    │              │ Phone 3    │ │
│  │ Toast      │              │ Modal      │              │ Sheet      │ │
│  │ ≈390×780px │              │ ≈390×780px │              │ ≈390×780px │ │
│  └────────────┘              └────────────┘              └────────────┘ │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │  底部标注区  高度≈80px                                              │ │
│  │  ① 提示信息·2.5秒自动关闭   ② 莫代尔·破坏性但温暖   ③ 底部页面·用于选项选择 │ │
│  └──────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 手机框架规格（三台相同）

| 属性 | 值 |
|------|-----|
| 手机框架外宽 | ≈390px（估算值） |
| 手机框架外高 | ≈780px（估算值） |
| 边框圆角 | ≈40px（估算值，iPhone 14 标准） |
| 边框颜色 | #FFFFFF（白色） |
| 边框厚度 | ≈8px（估算值） |
| 屏幕内容区宽 | ≈374px（估算值） |
| 状态栏高度 | ≈47px（iPhone 14 规范） |
| 底部 Home Indicator | ≈34px（iPhone 14 规范） |
| 背景渐变 | 浅粉→白→浅紫，从上到下渐变 |

---

## 三台手机并排布局

| 项目 | 值 |
|------|-----|
| 三机总宽（含间距） | ≈1440px（估算值） |
| 画布左右 padding | ≈48px 各侧（估算值） |
| 手机间隙 Gap | ≈64px（估算值） |
| 垂直对齐 | 顶部对齐（align-items: flex-start） |
| 水平分布 | 均匀分布（space-evenly 或手动均分） |

---

## Phone 1 — Toast 层级布局

```
Phone 1 内容区（374×686px 估算）
│
├── StatusBar（≈47px）
│   ├── 左：时间 "9:41"
│   └── 右：信号/WiFi/电池图标
│
├── NavigationBar（≈56px）
│   ├── 左：Avatar（圆形，≈36px）+ 名称"悠悠"+ 副标"在线·消息将自动发送"
│   └── 右：电话图标 + 更多图标（···）
│
├── ── TOAST 浮层（叠加于内容区顶部）──
│   ├── 距 NavBar 底部 ≈16px
│   ├── 宽：≈330px（估算值），水平居中
│   ├── 高：≈44px（估算值）
│   ├── 背景：#FFFFFF，圆角≈22px（全圆角）
│   ├── 阴影：轻微投影
│   ├── 左侧：粉色勾选图标（≈20px）
│   └── 文本："兑换成功，会员已激活。"
│
├── ContentArea（聊天内容区，≈500px高，估算）
│   └── 水晶心脏插画（居中，≈200px，估算）
│
└── InputBar（底部输入栏，≈56px）
    ├── 左：圆形加号按钮（≈32px）
    ├── 中：输入框 placeholder "想对悠悠说点什么呢..."
    └── 右：圆形语音/录音按钮（≈32px，粉色背景）
```

---

## Phone 2 — Modal 层级布局

```
Phone 2 内容区（374×686px 估算）
│
├── StatusBar（≈47px）
├── NavigationBar（≈56px）
│
├── ContentArea（带深色 scrim 遮罩）
│   ├── scrim：rgba(0,0,0,≈0.45)，覆盖整个内容区
│   └── ── MODAL CARD（叠加于 scrim 之上）──
│       ├── 水平居中，垂直居中于可见内容区
│       ├── 宽：≈306px（估算值）
│       ├── 高：≈280px（估算值）
│       ├── 背景：#FFFFFF，圆角≈20px
│       ├── 阴影：较明显投影
│       ├── padding：≈24px 四周
│       │
│       ├── 顶部插图区（≈64px高，估算）
│       │   └── 云朵 emoji 插图（带粉色爱心装饰，≈48px，估算）
│       │
│       ├── 标题（≈20px 字体）
│       │   └── "确认退出登录？"（加粗，#3A3A4A）
│       │
│       ├── 副标题（≈13px 字体）
│       │   └── "退出后需要重新通过邮箱链接登录。"（#888，居中）
│       │
│       ├── 主操作按钮（≈48px高，估算）
│       │   └── "确认退出"（粉色渐变填充，白色文字）
│       │
│       └── 次操作按钮（≈48px高，估算）
│           └── "取消"（白色背景/描边，深色文字）
│
└── InputBar（底部，被 scrim 覆盖，依然可见轮廓）
```

---

## Phone 3 — Bottom Sheet 层级布局

```
Phone 3 内容区（374×686px 估算）
│
├── StatusBar（≈47px）
├── NavigationBar（≈56px）
│
├── ContentArea（带轻度 scrim 遮罩）
│   ├── scrim：rgba(0,0,0,≈0.25）（比 Modal 浅）
│   └── 水晶心脏插画（透过 scrim 可见，位于上半区）
│
└── ── BOTTOM SHEET（从底部滑入）──
    ├── 位置：吸附屏幕底部
    ├── 宽：100%（全屏宽，≈374px）
    ├── 高：≈320px（估算值，约占屏幕高度 40%）
    ├── 背景：#FFFFFF，顶部圆角≈20px，底部无圆角（贴底边）
    ├── 顶部拖动条（Drag Handle）：≈36px 宽 × 4px 高，居中，颜色≈#D0D0D0
    │
    ├── 标题区（padding-top ≈16px）
    │   └── "选择主题"（≈17px，加粗，#3A3A4A，左对齐）
    │
    ├── 选项列表（单选 Radio Group）
    │   ├── 间距：≈12px per item（估算值）
    │   ├── 选项1："浅色"（Radio 已选中，粉色填充，≈20px）
    │   ├── 选项2："深色"（Radio 未选中，描边，≈20px）
    │   └── 选项3："跟随系统"（Radio 未选中，描边，≈20px）
    │
    └── 底部操作按钮区（padding-bottom ≈24px，含 safe-area）
        └── "完成"（全宽按钮，粉色渐变填充，≈48px高，圆角≈24px）
```

---

## 底部标注区布局

```
标注区（画布底部，高≈80px，y ≈ 940px 估算）

┌──────────────┬──────────────┬──────────────────────┐
│ ① 提示信息   │ ② 莫代尔     │ ③ 底部页面            │
│   ·2.5秒     │   ·破坏性但  │   ·用于选项选择       │
│   自动关闭   │   温暖       │                       │
└──────────────┴──────────────┴──────────────────────┘

每列居中对齐，水平间距与上方手机列对齐
圆形数字徽章：≈24px 直径，背景 #FFB7C5（cherry pink），文字白色 14px
文字：≈13px，#3A3A4A，水平居中
```

---

## Z-index 层次（每台手机内部）

| 层级 | 元素 | z-index 参考 |
|------|------|-------------|
| 0 | 聊天内容区 + 背景渐变 | base |
| 1 | 插图（水晶心脏） | 1 |
| 2 | InputBar | 2 |
| 3 | Scrim 遮罩（Modal/Sheet） | 100 |
| 4 | Modal Card / Bottom Sheet | 200 |
| 5 | Toast（独立浮层，不受 scrim 影响） | 300 |

---

## 滚动区域

所有三个组件展示均为静态展示状态，无需滚动。
Bottom Sheet 内选项列表当内容超出 Sheet 高度时可内部滚动（当前设计稿为3项，无溢出）。
