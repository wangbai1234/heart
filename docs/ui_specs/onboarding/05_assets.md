# 05 Assets — 首次引导 FirstVisitGuide

本文件覆盖 Onboarding 3-Step 的资源真相。旧版“拆成单独心形/云海/吊坠/礼盒资产”的建议失效。

## 1. 必须引用的复杂资源

| Step | 绝对路径 | 使用方式 |
|---|---|---|
| Step 1 | `/Users/wanglixun/heart/assets/backgrounds/首次引导页1.png` | 插画区 `contain`，顶部居中 |
| Step 2 | `/Users/wanglixun/heart/assets/backgrounds/首次引导页2.png` | 插画区 `contain`，顶部居中 |
| Step 3 | `/Users/wanglixun/heart/assets/backgrounds/首次引导页3.png` | 插画区 `contain`，顶部居中 |

## 2. 资源使用规则

- 三步引导插画必须使用上表中的三张现成图片
- 不允许把它们拆成“心形”“玻璃罩”“礼盒”“小水晶心”等多资源重新组合
- 不允许重新生成任一步插画
- 不允许裁切掉主体

## 3. 页面背景规则

- 插画外的页面背景若需要延展，只能使用设计稿同色系纯色或现有 token
- 不允许再额外选择其他背景资源

## 4. 必须由组件实现的元素

- 标题
- 副标题
- 分页点
- “下一步”
- “开始体验”
- “我有兑换码”

## 5. 禁止事项

- 禁止重画插画
- 禁止擅自增加飘花、粒子、云层资源
- 禁止把三步改成 Carousel 之外的结构
