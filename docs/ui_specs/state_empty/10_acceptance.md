# 10 Acceptance Checklist — 加载 / 空状态

> 目标视觉一致度：≥ 95%
> 检验方式：与设计稿 18_state_empty.png 逐元素对照
> 格式：□ = 待验收，✅ = 通过，❌ = 不通过

---

## A. 全局 / 页面级

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 双区域是否统一复用 `/Users/wanglixun/heart/assets/backgrounds/亮色背景图.png` | 视觉对比 |
| □ | 双区域均保持同一背景来源，无分割线可见 | 视觉对比 |
| □ | 整体无多余边框、分割线、阴影出现在背景上 | 视觉对比 |
| □ | 字体为 PingFang SC 或 HarmonyOS Sans SC（中文），英文/数字为 SF Pro Rounded | 字体检查 |

---

## B. StatusBar（状态栏）

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 时间显示"9:41"，字体 Semibold，颜色 #3A3A4A | 设计稿截图对比 |
| □ | 右侧信号格、Wi-Fi、电量图标颜色与设计稿一致（深灰色调）| 视觉对比 |
| □ | 状态栏背景透明，与页面背景融合 | 视觉对比 |
| □ | 上下两个 StatusBar 样式完全一致 | 视觉对比 |

---

## C. ChatHeader（聊天顶栏）

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 返回箭头"<"可见，颜色约 #8A8A9A，触摸目标 ≥ 44pt | 视觉对比 + 测量 |
| □ | 角色头像为圆形，直径约 88px，有白色描边 | 视觉对比 |
| □ | 若运行态无头像，是否显示空白占位头像组件而非角色图 | 视觉对比 |
| □ | 角色名"悠悠"字体 Semibold，颜色 #3A3A4A | 视觉对比 |
| □ | 空状态副标题"在线 · 愿意倾听你的一切"完整显示，无截断 | 视觉对比 |
| □ | 加载状态副标题显示"···"（三点动画，或静态"..."） | 视觉对比 |
| □ | 电话图标和更多菜单图标均可见，颜色约 #8A8A9A | 视觉对比 |
| □ | Header 垂直居中对齐，左右元素不超出边界 | 视觉对比 |

---

## D. 空聊天状态 — 插画区

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 空状态中心视觉是否由组件实现，而非新生成插画资源 | 视觉对比 |
| □ | 云朵光晕背景可见，粉紫渐变，边缘柔化无硬边 | 视觉对比 |
| □ | 云朵背景整体为椭圆扩散形，水平居中 | 视觉对比 |
| □ | 心形宝石插画居于云朵上方，视觉上"托举"效果明显 | 视觉对比 |
| □ | 心形宝石颜色符合设计（粉→紫→蓝渐变，有高光白点）| 视觉对比 |
| □ | 心形宝石整体居中，无偏移 | 视觉对比 |
| □ | 心形与云朵的层级正确（心形在上，云朵在下）| 视觉对比 |
| □ | 插画区无多余文字或 UI 元素介入 | 视觉对比 |

---

## E. 空聊天状态 — 引导文字

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 文字内容为"我们刚认识，先聊点什么吧？"，无错字 | 文字核对 |
| □ | 文字水平居中对齐 | 视觉对比 |
| □ | 字号与设计稿视觉一致（约 18pt，中等大小）| 视觉对比 |
| □ | 字重为 Medium（不偏细、不偏粗）| 视觉对比 |
| □ | 颜色为 #3A3A4A（深炭色，非纯黑）| 色值核对 |
| □ | 文字与插画区之间间距与设计稿一致（约 32pt）| 视觉对比 |

---

## F. 空聊天状态 — 建议 Pill 行

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | Pill 行整体水平居中 | 视觉对比 |
| □ | 三个 Pill 水平排列，间距均匀（约 16pt）| 视觉对比 |
| □ | 每个 Pill 为胶囊形圆角（圆角等于高度一半）| 视觉对比 |
| □ | Pill 背景为浅白/磨砂白（约 rgba(255,255,255,0.80））| 视觉对比 |
| □ | Pill 有细描边（约 1.5px，粉色调低透明度）| 视觉对比 |
| □ | Pill 有轻微投影（粉色调）| 视觉对比 |
| □ | Pill 1 图标为笑脸圆形，颜色 cherry pink | 视觉对比 |
| □ | Pill 1 文字为"今天心情如何？"，无错字 | 文字核对 |
| □ | Pill 2 图标为对话气泡，颜色 cherry pink | 视觉对比 |
| □ | Pill 2 文字为"陪我说话"，无错字 | 文字核对 |
| □ | Pill 3 图标为五角星，颜色金黄（约 #E8C84A）| 视觉对比 |
| □ | Pill 3 文字为"给我讲个故事"，无错字 | 文字核对 |
| □ | 三个 Pill 高度一致（约 64px）| 视觉对比 |
| □ | Pill 文字大小与设计稿一致 | 视觉对比 |
| □ | Pill 触摸目标高度 ≥ 44pt（已达标，高度 64px）| 测量 |

---

## G. 加载状态 — 骨架气泡

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 骨架气泡 1 左对齐（AI），头像占位圆在左 | 视觉对比 |
| □ | 骨架气泡 2 右对齐（用户），头像占位圆在右 | 视觉对比 |
| □ | 骨架气泡 3 左对齐（AI），包含两行不等宽矩形 | 视觉对比 |
| □ | 骨架头像占位圆为完整圆形 | 视觉对比 |
| □ | 骨架矩形圆角与设计稿一致（约 14–16px）| 视觉对比 |
| □ | 骨架元素颜色与背景有可见区分，但色差不刺眼（同色系）| 视觉对比 |
| □ | 骨架矩形宽度比例与设计稿一致（约 AI 480px，用户 420px，多行第二条 300px）| 视觉对比 |
| □ | 三组骨架气泡垂直间距均匀（约 32pt）| 视觉对比 |
| □ | Shimmer 扫光动画方向为从左到右 | 功能验收 |
| □ | Shimmer 动画流畅，无卡顿（60fps）| 功能验收 |
| □ | Shimmer 三组骨架同步启动（无错位感）| 功能验收 |

---

## H. 加载状态 — Header 打点动画

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 加载状态 Header 副标题显示"···"（三点）| 视觉对比 |
| □ | 打点动画为三个点依次出现（顺序淡入）| 功能验收 |
| □ | 打点动画循环，无停顿 | 功能验收 |
| □ | 打点动画节奏自然（约 900ms 一周期）| 功能验收 |

---

## I. 加载状态 — InputBar

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | "+" 按钮为圆形描边，颜色偏灰（约 #A8A8B8）| 视觉对比 |
| □ | 输入框占位文字"想对悠悠说什么呢..."完整显示，无截断 | 文字核对 |
| □ | 占位文字颜色偏淡（约 #C8B6C8，玫瑰灰）| 视觉对比 |
| □ | 麦克风按钮为圆形描边，颜色为 cherry pink（约 #FFB7C5）| 视觉对比 |
| □ | InputBar 底部安全区距离正确（约 34pt）| 测量 |
| □ | InputBar 左右 Padding 与设计稿一致 | 视觉对比 |
| □ | Header / Empty / Skeleton 是否未引用不存在的 `illus_*` 或 `avatar_youyou*` 资源 | 代码审查 |

---

## J. 响应式 / 尺寸一致性

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 在 iPhone 14（390pt 宽）上整体布局无溢出 | 真机/模拟器验收 |
| □ | 在 iPhone SE（375pt 宽）上 Pill 行无换行或过度压缩 | 模拟器验收 |
| □ | 在 iPhone 15 Pro Max（430pt 宽）上布局居中对齐，无异常拉伸 | 模拟器验收 |
| □ | Safe Area Top/Bottom 已正确处理，内容无遮挡 | 真机验收 |

---

## K. 交互功能

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 点击 Pill 后 Pressed 状态视觉反馈正确（缩小 + 背景略深）| 交互验收 |
| □ | 点击 Pill 后消息正确填入并发送 | 功能验收 |
| □ | 发送后空状态区平滑过渡到加载状态区 | 交互验收 |
| □ | AI 响应到达后骨架正确消失，真实气泡正确出现 | 功能验收 |
| □ | 点击返回按钮正确返回上一页 | 功能验收 |

---

## L. 无障碍验收

| # | 检查项 | 基准值 |
|---|--------|--------|
| □ | 插画、骨架等装饰性元素已设置 aria-hidden="true" | 代码审查 |
| □ | 骨架加载区域有 role="status" 和适当 aria-label | 代码审查 |
| □ | 打点动画文字有适当 aria-live 播报 | 代码审查 |
| □ | 三个 Pill 均有清晰的 aria-label（包含"发送："前缀）| 代码审查 |
| □ | VoiceOver 焦点顺序与视觉顺序一致 | 真机 VoiceOver 验收 |
| □ | Reduce Motion 开启时动画停止或简化 | 功能验收 |
| □ | 所有交互元素对比度满足 WCAG 3:1（非文本）或 4.5:1（文本）| 对比度工具检查 |

---

## 视觉一致度自评

| 区域 | 目标一致度 | 说明 |
|------|-----------|------|
| StatusBar | 100% | 系统原生，无设计偏差 |
| ChatHeader | ≥ 97% | 结构简单，可严格对齐 |
| 空状态插画区 | ≥ 90% | 插画为图片资产，比例和颜色需精确裁切 |
| 引导文字 | ≥ 99% | 字体/字号/间距需精确 |
| Pill 行 | ≥ 95% | 圆角/阴影/颜色需精确 |
| 骨架屏 | ≥ 95% | 尺寸/颜色/圆角需精确，Shimmer 动画流畅 |
| InputBar | ≥ 97% | 结构简单 |
| **整体** | **≥ 95%** | 目标达标线 |
