# 04 组件分析 — Style Tile（视觉总谱）

> 逐一分析 Style Tile 中每个可见组件，提取尺寸、样式、状态与复用建议。
> 带（估算值）标注者为目视估算，PNG 为单一参考源。

---

## 组件 0 — Logo 文字标识

| 属性 | 值 |
|------|----|
| **组件名** | `AppLogo` |
| **作用** | 品牌识别标识，出现在启动页、导航栏、设置等屏幕 |
| **层级** | 画布顶层（z=15） |
| **内容** | 文字 "yuoyuo"（全小写，不得大写） |
| **字体** | 圆润无衬线，约 PingFang SC Bold 或定制字重（估算值） |
| **字号** | 约 36px（估算值） |
| **字色** | `#3A3A4A`（`color-ink`） |
| **字距** | 约 1px（estimat值，宽松感） |
| **位置** | 距左 32px，距顶 40px（估算值） |
| **状态** | 仅展示态，无交互 |
| **是否可复用** | ✅ 是，建议封装为 `AppLogo` 组件，支持 `size: sm/md/lg` |

---

## 组件 1.1 — 调色板卡片容器

| 属性 | 值 |
|------|----|
| **组件名** | `SectionCard`（通用卡片容器） |
| **作用** | 承载各视觉规范区块内容的白色半透明容器 |
| **尺寸** | 宽约 48% 画布，高约 190px（估算值） |
| **背景** | `rgba(255,255,255,0.80)`（估算值） |
| **Radius** | `radius-xl`（约 20px） |
| **Shadow** | `shadow-card`（`0 4px 16px rgba(0,0,0,0.06)`） |
| **Padding** | 约 20px（估算值） |
| **是否可复用** | ✅ 是，`SectionCard` 可通用于所有区块容器 |

---

## 组件 1.2 — 颜色椭圆（Color Swatch）

| 属性 | 值 |
|------|----|
| **组件名** | `ColorSwatch` |
| **作用** | 视觉化呈现品牌色彩 Token |
| **数量** | 6 枚（从左至右：#FFB7C5、#A7C7E7、#C8B6FF、#FFF8F3、#3A3A4A、rgba(255,255,255,0.55)） |
| **形状** | 竖向椭圆（Pill 形，宽约 70px × 高约 90px，估算值） |
| **Radius** | `radius-pill`（全圆角椭圆） |
| **Border** | 最后一个 Glass 色块（rgba 值）有浅灰边框（约 `1px solid rgba(0,0,0,0.10)`，估算值），其余无描边 |
| **阴影** | 无明显阴影 |
| **标注** | 每枚椭圆正下方为 Hex 文字，字号约 10px（估算值），颜色 `color-ink-secondary` |
| **最后一枚特殊** | Glass 椭圆：白色半透明填充（可见底部粉色渐变透出），标注为 `rgba(255,255,255,.55)` |
| **状态** | 仅展示态 |
| **是否可复用** | ✅ 是，可作 `ColorSwatchItem` 组件 |

---

## 组件 2 — 字体示例组（Typography Showcase）

| 属性 | 值 |
|------|----|
| **组件名** | `TypographyShowcase` |
| **作用** | 展示中英文字体层级 |
| **内容层级** | 1. 大标题（"你今天还好吗"，约 40px Bold）2. 正文（"你今天还好吗"，约 16px Regular + "PingFang SC Regular" 字体名）3. 英文大标题（"Hello, yuoyuo."，约 20px Bold）4. 英文字体名（"SF Pro Rounded"，约 14px Regular） |
| **布局** | 竖向堆叠，间距约 12px（估算值） |
| **颜色** | 全部使用 `color-ink`（#3A3A4A） |
| **状态** | 仅展示态 |
| **是否可复用** | 否，此为文档展示组件，非产品 UI 组件 |

---

## 组件 3.1 — AI 气泡（Left Bubble）

| 属性 | 值 |
|------|----|
| **组件名** | `ChatBubbleAI` |
| **作用** | 展示 AI 角色发送的聊天消息 |
| **位置** | 卡片左侧，靠左对齐 |
| **背景** | 白色半透明（约 `rgba(255,255,255,0.85)`，估算值） |
| **Radius** | 约 16px，右下角约 4px（"小尾巴"方向，估算值） |
| **宽度** | 约 48% 气泡卡片内宽（估算值） |
| **Shadow** | `shadow-bubble` |
| **内容结构** | 顶部：发送者名 "yuoyuo"（粉色，`color-primary`，约 12px Semibold）；中部：消息正文（约 14px Regular，`color-ink`）；底部右：时间戳（约 10px，`color-ink-secondary`） |
| **示例内容** | 名: "yuoyuo" / 正文: "早呀，今天也要开心地度过呢～✨" / 时间: "09:30" |
| **Emoji 支持** | ✅（✨ 可见） |
| **状态** | 展示态（无 loading/error 状态示意） |
| **是否可复用** | ✅ 是，核心聊天组件，需支持 `sender: 'ai' | 'user'` prop |

---

## 组件 3.2 — 用户气泡（Right Bubble）

| 属性 | 值 |
|------|----|
| **组件名** | `ChatBubbleUser` |
| **作用** | 展示用户发送的聊天消息 |
| **位置** | 卡片右侧，靠右对齐 |
| **背景** | 天蓝半透明（约 `#A7C7E7` 80% 不透明度，估算值） |
| **Radius** | 约 16px，左下角约 4px（估算值） |
| **宽度** | 约 48% 气泡卡片内宽（估算值） |
| **Shadow** | `shadow-bubble` |
| **内容结构** | 消息正文（约 14px Regular，`color-ink`）；底部右：时间戳 + 已读对勾（"09:32 ✓"） |
| **示例内容** | 正文: "谢谢yuoyuo，你真是最好的陪伴！" / 时间: "09:32 ✓" |
| **已读标记** | "✓" 蓝色小勾（约 `#A7C7E7` 加深版，估算值） |
| **状态** | 展示态 |
| **是否可复用** | ✅ 是，与 `ChatBubbleAI` 共享基类，通过 prop 控制方向 |

---

## 组件 4.1 — Primary 按钮（确认）

| 属性 | 值 |
|------|----|
| **组件名** | `ButtonPrimary` |
| **作用** | 主要行动按钮，触发核心操作 |
| **尺寸** | 宽约 200px × 高约 48px（估算值） |
| **形状** | 胶囊形（`radius-pill`） |
| **背景** | `gradient-button-primary`（#FFB7C5 渐变，略有光泽感） |
| **文字** | "确认"，约 16px SemiBold，白色（`#FFFFFF`） |
| **Shadow** | `shadow-button-primary`（粉色外发光 `0 4px 12px rgba(255,183,197,0.45)`） |
| **状态** | 默认展示态；Pressed 态：加深 10%（设计稿未定义） |
| **内边距** | 约 `12px 32px`（估算值） |
| **是否可复用** | ✅ 是，最高优先级复用组件 |

---

## 组件 4.2 — Secondary 按钮（取消）

| 属性 | 值 |
|------|----|
| **组件名** | `ButtonSecondary` |
| **作用** | 次要操作按钮，提供取消/退出等选项 |
| **尺寸** | 宽约 200px × 高约 48px（估算值），与 Primary 等宽 |
| **形状** | 胶囊形（`radius-pill`） |
| **背景** | 白色（`#FFFFFF` 或 `rgba(255,255,255,0.90)`，估算值） |
| **Border** | 浅灰边框（约 `1px solid rgba(0,0,0,0.08)`，估算值） |
| **文字** | "取消"，约 16px Regular，`color-ink`（#3A3A4A） |
| **Shadow** | `shadow-button-secondary` |
| **状态** | 默认展示态 |
| **是否可复用** | ✅ 是 |

---

## 组件 4.3 — Text Link 按钮（了解更多）

| 属性 | 值 |
|------|----|
| **组件名** | `ButtonTextLink` |
| **作用** | 无背景的文字链接按钮，用于次级导航或信息补充 |
| **背景** | 无 |
| **文字** | "了解更多"，约 14px Regular，`color-accent`（#C8B6FF，薰衣草紫） |
| **下划线** | 无下划线（设计稿视觉无下划线，估算值） |
| **状态** | 默认展示态；Pressed 态：颜色加深（设计稿未定义） |
| **是否可复用** | ✅ 是 |

---

## 组件 5 — AI 角色卡片（Character Card）

| 属性 | 值 |
|------|----|
| **组件名** | `CharacterCard` |
| **作用** | 展示 AI 角色信息、等级、统计数据与个性签名 |
| **整体尺寸** | 约 48% 画布宽 × 约 210px 高（估算值） |
| **背景** | 与 SectionCard 同（白色半透明） |
| **Radius** | `radius-xl` |
| **内部布局** | 水平 Flex（头像 left + 信息区 right），底部独占一行 motto |

### 5.1 — 头像（Avatar）

| 属性 | 值 |
|------|----|
| **组件名** | `CharacterAvatar` |
| **形状** | 正圆形（`radius-full`） |
| **尺寸** | 约 88px × 88px（估算值） |
| **内容** | 二次元少女立绘（粉发、紫眼、花朵发饰） |
| **背景圆** | 浅粉白渐变圆（头像底盘，约 96px，估算值） |
| **Shadow** | `shadow-avatar`（粉色光晕） |
| **Border** | 约 `2px solid rgba(255,255,255,0.80)`（白色内边框，估算值） |

### 5.2 — 角色名 + 等级

| 属性 | 值 |
|------|----|
| **角色名** | "yuoyuo"，约 18px SemiBold，`color-ink` |
| **等级徽章** | "Lv.12"，约 12px，背景约 `rgba(255,183,197,0.20)`（粉色浅底），`color-primary`，Radius 约 8px（估算值） |
| **布局** | 横向排列，角色名 + 等级间距约 8px |

### 5.3 — 简介文字

| 属性 | 值 |
|------|----|
| **内容** | "与你相遇，是最美好的奇迹。" |
| **字号** | 约 13px（估算值） |
| **颜色** | `color-ink-secondary` |

### 5.4 — 统计数据行

| 属性 | 值 |
|------|----|
| **内容** | 爱心图标 + "1250 陪伴天数" + 星星图标 + "3560 温暖时刻" |
| **爱心图标** | 粉色实心爱心，约 14px |
| **星星图标** | 空心星星，约 14px，淡紫/灰色 |
| **数字字号** | 约 16px Bold，`color-ink` |
| **标签字号** | 约 11px，`color-ink-secondary` |
| **布局** | 两组数据横向排列，间距约 16px |

### 5.5 — Motto 行

| 属性 | 值 |
|------|----|
| **内容** | "✨ 愿每一天都充满温柔与惊喜。" |
| **字号** | 约 12px（估算值） |
| **颜色** | `color-ink-secondary` 或浅粉 |
| **位置** | 卡片底部，占满宽度 |
| **背景** | 可能有极浅粉色底（约 `rgba(255,183,197,0.08)`，估算值） |

---

## 组件 6 — 底部导航图标条（Bottom Navigation Bar）

| 属性 | 值 |
|------|----|
| **组件名** | `BottomNavBar` |
| **作用** | App 底部主导航，5 个 Tab |
| **整体宽度** | 约 48% 画布宽（此处为展示区块，实际 App 中为全宽） |
| **高度** | 约 80px（估算值） |
| **背景** | 与 SectionCard 同 |

### 图标列表

| 序号 | Icon 形态 | 标签 | 状态 |
|------|-----------|------|------|
| 1 | 房子形 outline Icon | 首页 | 默认（未选中） |
| 2 | 对话气泡 outline Icon | 聊天 | 默认 |
| 3 | 麦克风 + 斜线（关闭） outline Icon | 麦克风（关闭） | 特殊状态（禁用/关闭） |
| 4 | 礼物盒 outline Icon | 礼物（兑换） | 默认 |
| 5 | 齿轮 outline Icon | 齿轮 | 默认 |
| 6 | 人形轮廓 outline Icon | 个人资料 | 默认 |

> 注：Style Tile 展示了 **6 枚 Icon**，但实际 App 底部导航通常为 5 Tab。麦克风（关闭）可能是状态图标而非独立 Tab。

| 属性 | 值 |
|------|----|
| **Icon 尺寸** | 约 24px × 24px（估算值） |
| **Icon 颜色** | `color-ink-secondary`（约 #7A7A8A），未选中态 |
| **选中 Icon 颜色** | `color-primary`（#FFB7C5，设计稿未展示选中态） |
| **标签字号** | 约 11px（估算值） |
| **标签颜色** | `color-ink-secondary` |
| **Icon + 标签间距** | 约 4px（估算值） |
| **每个 Tab 宽度** | 等宽均分（估算值） |
| **是否可复用** | ✅ 是，App 核心导航组件 |

---

## 组件 7 — 背景插画（Illustration Background）

| 属性 | 值 |
|------|----|
| **组件名** | `IllustrationBackground` |
| **作用** | 梦幻场景渲染，塑造品牌氛围，非功能性 |
| **内容** | 城堡建筑背景 + 粉色蓝色花朵（前景） + 蝴蝶装饰 |
| **位置** | 画布底部全宽区域 |
| **高度** | 约 260px（估算值） |
| **颜色调性** | 粉（#FFB7C5）+ 蓝（#A7C7E7）+ 白（#FFFFFF）混调 |
| **透明度** | 顶部渐出（约 60% → 100% 透明度，估算值） |
| **是否可复用** | ✅ 是，可用于启动页、登录页、空状态页等场景 |

---

## 可复用组件优先级清单

| 优先级 | 组件名 | 重要性 |
|--------|--------|--------|
| P0 | `ChatBubbleAI` / `ChatBubbleUser` | 核心聊天功能 |
| P0 | `ButtonPrimary` | 全局主操作 |
| P0 | `BottomNavBar` | 全局导航 |
| P1 | `CharacterCard` | 角色展示页 |
| P1 | `CharacterAvatar` | 角色头像（多处复用） |
| P1 | `ButtonSecondary` / `ButtonTextLink` | 全局次级操作 |
| P2 | `SectionCard` | 容器组件 |
| P2 | `AppLogo` | 品牌标识 |
| P3 | `ColorSwatch` | 仅设计稿内使用 |
| P3 | `IllustrationBackground` | 装饰性背景 |
