# Include main requirements
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.14.0
hypothesis>=6.92.0
aiosqlite>=0.20.0

# E2E (Tier E) — real HTTP + Playwright APIRequestContext, ready for browser tests
pytest-playwright>=0.5.0
playwright>=1.45.0

# Load testing
locust>=2.20.0

# Code quality — pinned exact so CI == local (see scripts/ci.sh stage_lint).
# Unpinned ruff drifts to latest and reformats .md code blocks older ruff skips.
ruff==0.15.13
mypy==2.1.0

# Interactive development
ipython>=8.20.0
