[pytest]
# Tier C Live test configuration
# Used by: pytest -m live --live -c tests/live/.pytest_live.ini tests/live/

addopts = -m live --live --max-cost=2.0 -v

markers =
    live: Tier C live tests (real LLM, cost-capped)
    drift: voice drift regression tests
